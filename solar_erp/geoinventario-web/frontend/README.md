# Frontend

Base inicial del frontend con React + TypeScript + Vite.

## Enfoque

- estructura por `app`, `components`, `features`, `pages`, `services` y `types`
- base lista para auth, dashboard e inventario
- estilos simples y neutros para arrancar el MVP
